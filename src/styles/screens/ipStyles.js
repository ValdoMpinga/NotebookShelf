import { StyleSheet, Dimensions } from 'react-native';
import { Colors } from '../../utils/constants';

export default ipStyles = StyleSheet.create({

    title: {
        fontFamily: 'Monaco',
        fontSize: 20,
        color: Colors.white,
        marginTop: 20,
        flex:1,
    },
    ipText: {
        fontFamily: 'Monaco',
        fontSize: 20,
        color: Colors.yellow,
        fontWeight: 'bold',
        marginTop: 20,
        flex: 1,

    }
})
